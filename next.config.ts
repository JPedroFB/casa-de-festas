import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['i.pravatar.cc'], // Permitindo o domínio pravatar para avatares de teste
  }
};

export default nextConfig;
